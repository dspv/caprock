version: 2
project_name: caprock

before:
  hooks:
    - go mod tidy

builds:
  - id: caprock
    main: ./cmd/caprock
    binary: caprock
    env: [CGO_ENABLED=0]
    flags: [-trimpath]
    ldflags: ["-s -w -X github.com/dspv/caprock/internal/version.Version={{.Version}}"]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
  - id: caprock-hook
    main: ./cmd/caprock-hook
    binary: caprock-hook
    env: [CGO_ENABLED=0]
    flags: [-trimpath]
    ldflags: ["-s -w -X github.com/dspv/caprock/internal/version.Version={{.Version}}"]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]

universal_binaries:
  - id: caprock-universal
    ids: [caprock]
    replace: false
  - id: caprock-hook-universal
    ids: [caprock-hook]
    replace: false

archives:
  - id: default
    ids: [caprock, caprock-hook]
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files: [LICENSE, README.md]

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc
  filters:
    exclude: ["^docs", "^chore", "^ci"]

homebrew_casks:
  - name: caprock
    # Tap repo dspv/homebrew-tap (public); the cask is pushed here on release.
    repository:
      owner: dspv
      name: homebrew-tap
      # The default GITHUB_TOKEN cannot write to another repo (the tap), so use a
      # dedicated PAT with contents:write on dspv/homebrew-tap, provided as the
      # HOMEBREW_TAP_TOKEN secret. If the secret is unset the value is empty and
      # skip_upload:auto below skips the push instead of failing the release.
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://caprock.dev"
    description: "Mission control for Claude Code — watch, control and orchestrate your sessions"
    # Skip the cask push on prereleases / when the tap is unavailable.
    skip_upload: auto
    caveats: |
      Start it with:  caprock up
      Then open:      http://127.0.0.1:4173

release:
  draft: true
  prerelease: auto
